# DESIGN.md — Policy Board

이 문서는 `apps/policy-board`의 시각 기준이다. 화면을 고칠 때는 먼저 이 파일을 읽고, 구현 후 모바일/태블릿/데스크톱에서 스크린샷을 확인한다.

## 제품 감각

- 공공정책 검색기가 아니라 "내가 되는 정책을 근거와 함께 판정해주는 작업 도구"처럼 느껴져야 한다.
- 마케팅 랜딩 페이지처럼 꾸미지 않는다. 첫 화면은 바로 판정/목록/프로필 작업으로 이어져야 한다.
- 사용자는 정책명보다 상태, 마감, 지원금, 탈락/보류 사유를 빠르게 스캔해야 한다.
- 비공식 도구라는 신뢰 한계를 숨기지 말고, 원문 확인과 조회일을 가까운 곳에 둔다.

## 팔레트

라이트 모드:

- 배경 `#F5F7FA`
- 표면 `#FFFFFF`
- 보조 표면 `#EDF1F6`
- 본문 `#101720`
- 보조 본문 `#415061`, 약한 본문 `#5C6A7A`
- 선 `#DDE4EC`, 강한 선 `#C9D4E0`
- 주요 액션/대기 `#1B4FD9`
- 가능 `#0B7A55`
- 주의/마감임박 `#B8430E`

다크 모드:

- 배경 `#0C1017`
- 표면 `#141B25`
- 보조 표면 `#1D2632`
- 본문 `#E8EDF4`
- 보조 본문 `#A3B0C0`, 약한 본문 `#8695A8`
- 선 `#263140`, 강한 선 `#33404F`
- 주요 액션/대기 `#6A9BFF`
- 가능 `#3FCB96`
- 주의/마감임박 `#FF8B52`

## 타이포그래피

- 시스템 산세리프를 사용한다: Apple SD Gothic Neo, Pretendard, Noto Sans KR 계열.
- 본문 기본은 15px/1.6이다.
- 카드 제목은 16px 근처, 섹션 제목은 16-18px 근처로 유지한다.
- 히어로급 큰 글자는 첫 판정 결과처럼 진짜 우선순위가 있는 곳에만 쓴다.
- `letter-spacing`은 0 또는 약한 음수만 사용한다. 작은 라벨의 대문자 느낌에는 약한 양수만 허용한다.

## 간격과 형태

- 기본 페이지 좌우 여백은 모바일 16-20px, 데스크톱 최대 폭은 1120px이다.
- 반복 카드 간격은 12px를 기준으로 한다.
- 카드/패널 반경은 8-10px 범위에 둔다. 장식적인 둥근 카드 감성으로 흐르지 않는다.
- 터치 대상은 최소 44px 높이를 확보한다.
- 탭, 버튼, 칩, 링크는 hover/focus/selected 상태가 구분되어야 한다.

## 컴포넌트 기준

- 정책 카드는 상태 바, 상태 pill, D-day, 제목, 기관, 금액, 자격요약, 원문/조회일이 한눈에 들어와야 한다.
- 판정 사유는 가능/보류/탈락/대기 상태별 색이 즉시 읽혀야 한다.
- 필터는 칩으로 유지하되, 모바일에서 줄바꿈되어도 레이아웃이 흔들리지 않아야 한다.
- 프로필 입력은 30초 안에 끝나는 밀도의 폼이어야 한다. 빈 값이 많아도 사용자가 죄책감을 느끼지 않게 쓴다.
- 빈 상태는 조용하고 짧게 말한다. 사용법 설명을 길게 붙이지 않는다.

## Do

- 같은 의미에는 같은 색과 같은 형태를 반복한다.
- 숫자, D-day, 조회일은 tabular number로 정렬감을 준다.
- 작은 화면에서 가장 먼저 보이는 정보는 "지금 1순위" 또는 "내 판정"이어야 한다.
- 불확실한 정보에는 `확인 필요` 성격의 표시를 붙인다.
- 수정 후에는 최소 390px, 768px, 1280px 뷰포트로 확인한다.

## Don't

- 보라/남색 그라데이션, 장식용 배경, 랜딩 페이지식 hero를 추가하지 않는다.
- 카드 안에 카드처럼 보이는 중첩 패널을 과하게 만들지 않는다.
- 정책 데이터보다 앱 설명이 더 크게 보이게 하지 않는다.
- 한 화면에 새 색을 즉흥적으로 추가하지 않는다. 필요한 색은 CSS 변수로 승격한다.
- `index.html` 하나짜리 구조를 깨기 위해 빌드 파이프라인을 도입하지 않는다.

## 비주얼 QA 루프

1. 로컬 서버를 켠다: `python3 serve.py`
2. 콘솔 오류와 네트워크 실패를 먼저 확인한다.
3. `390x844`, `768x1024`, `1280x900`에서 스크린샷을 찍는다.
4. 다음을 스스로 비평한다: 대비, 정렬, 터치 영역, 정보 우선순위, 빈 상태, 줄바꿈, 가로 스크롤.
5. 작은 수정 후 같은 뷰포트를 다시 찍어 차이를 확인한다.

합격 기준: 주요 탭 전환과 프로필 입력이 가능하고, 세 뷰포트 모두에서 텍스트 겹침/잘림/불필요한 가로 스크롤이 없어야 한다.
